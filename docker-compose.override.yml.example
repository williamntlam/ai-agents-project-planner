# Example override file for custom configurations
# Copy this to docker-compose.override.yml and customize as needed
# docker-compose.override.yml is automatically loaded by docker-compose

version: '3.8'

services:
  etl_pipeline:
    # Override command for different environments
    command: ["python", "-m", "etl_pipeline.main", "--env", "prod", "--verbose"]
    
    # Add additional environment variables
    environment:
      ETL_ENV: prod
      # Add other env vars as needed
    
    # Override volumes if needed
    # volumes:
    #   - ./data:/app/data:ro
    #   - ./etl_pipeline/config:/app/etl_pipeline/config:ro
    #   - ./logs:/app/logs

